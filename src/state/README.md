# State inspector (planned)

Future home for state-management logging adapters (Redux / RTK middleware, optional Jotai).

v1 ships network inspector only. Panel shell is tab-ready for a `Network | State` split later.
